2025/11/14
密码的GD的命名规则和我的习惯好不一样。。
我才不喜欢用下划线。。要我自己写，我就写SetMaxHealth（）了，变量我就写maxHealth

已经定义了基础最大生命值、实际最大生命值和当前生命值

！！！定义了一个设置基础最大生命值的函数set_max_health，但需要继续完善：
1.调用这个函数后要重新对实际最大生命值计算更新。
2.可能要对当前生命值做修改

！！！定义了一个用于治疗的函数treat，但过于简陋。我希望它有更多的模式，比如按百分比治疗等

定义了一个函数get_health_percentage，用于获取当前生命值的百分比。这个函数没啥问题了（吧），我暂时不想改。

思索buff的实现方法。。
下一步定义：防御力
下一步定义：hurt函数，用于减少当前生命值。接受参数：（原始伤害，伤害类型，对伤害来源的引用）